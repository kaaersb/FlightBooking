<Window x:Class="GUI.Views.ProfileView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI.Views"
        mc:Ignorable="d"
        Title="ProfileView" Height="450" Width="800">
    <DockPanel Margin="10">
        <Button Content="← Tilbage"
                DockPanel.Dock="Top"
                Width="75"
                Margin="0,0,0,10"
                Click="Tilbage_Click" />

        <ListView x:Name="UserBookings">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Flight No." DisplayMemberBinding="{Binding OutboundFlight.FlightNumber}" Width="80"/>
                    <GridViewColumn Header="From" DisplayMemberBinding="{Binding OutboundFlight.Origin}" Width="100"/>
                    <GridViewColumn Header="To" DisplayMemberBinding="{Binding OutboundFlight.Destination}" Width="100"/>
                    <GridViewColumn Header="Date" DisplayMemberBinding="{Binding OutboundFlight.DepartureUtc, StringFormat=d}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Window>
